# Snowflake Streamlit Environment Configuration
# This file is used when deploying to Snowflake Streamlit

name: streamlit
channels:
  - snowflake
dependencies:
  - streamlit>=1.28.0
  - pandas>=1.5.0
  - snowflake-snowpark-python>=1.9.0

